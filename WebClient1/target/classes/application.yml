server:
  port: 8080
spring:
  application:
    name: WebClient1
  datasource:
    name: users
    generate-unique-name: false
  data:
    rest:
      base-path: /default-api
  thymeleaf:
    cache: false
  jpa:
    show-sql: false
    hibernate:
      show-sql: false
    properties:
      hibernate:
        format_sql: false

